 - Removed <ini name="apc.enable_cli" value="1"/> from phpunit.xml.dist, as it was causing warnings.
 For now I set it permanently in my .ini
 - Table tests
 - figure out static code analysis
 - think about some functionality that would require playing with routes. Maybe some "double parameters"?
    /buckets/{bucket_id}/{droplet_id}. This would also give me a chance to go through a composite db key flow.
 - add some advanced query logic, to play with ORM more
 - go again through https://book.cakephp.org/5.x/tutorials-and-examples/cms/tags-and-users.html as I've pretty much just
    flipped through it, to get to auth part
